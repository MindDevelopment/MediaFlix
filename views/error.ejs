<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - MediaFlix</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #141414;
            color: white;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .error-container {
            max-width: 800px;
            margin: 100px auto;
            padding: 30px;
            background: #1f1f1f;
            border-radius: 8px;
            text-align: center;
        }
        .error-icon {
            font-size: 60px;
            color: #E50914;
        }
        .error-message {
            margin-top: 20px;
        }
        .error-stack {
            margin-top: 30px;
            text-align: left;
            background: #2f2f2f;
            padding: 15px;
            border-radius: 4px;
            overflow-x: auto;
            font-family: monospace;
            font-size: 14px;
        }
        .btn-home {
            background: #E50914;
            border: none;
            margin-top: 20px;
        }
        .btn-home:hover {
            background: #b20710;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="error-container">
            <div class="error-icon">
                <i class="fas fa-exclamation-triangle"></i>
            </div>
            <h1 class="mt-3">Oops! Something went wrong</h1>
            <div class="error-message">
                <p><%= error %></p>
            </div>
            <% if (stack) { %>
                <div class="error-stack">
                    <pre><%= stack %></pre>
                </div>
            <% } %>
            <a href="/dashboard" class="btn btn-home">Return to Dashboard</a>
        </div>
    </div>
</body>
</html>
